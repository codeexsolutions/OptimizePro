<Project Sdk="Microsoft.NET.Sdk">

  <!--
    ATENÇÃO — LICENÇA GPL-3.0-or-later (02/09/2026, "plano B" do módulo Vetor):

    Este projeto usa BitmapToVector/BitmapToVector.SkiaSharp (porte em C# do Potrace),
    licenciado GPL-3.0-or-later. Por isso ele é um EXECUTÁVEL SEPARADO, de propósito —
    nunca deve ser referenciado como ProjectReference/PackageReference por Optimize.App
    nem por qualquer outro projeto fechado da solução. O app principal só INVOCA este
    .exe como processo externo (Process.Start, comunicação por arquivo/stdout — nunca
    por chamada de função in-process), o que mantém o app principal fora do "linking"
    que o GPL trata como obra combinada. Ver OptimizePro.Services/Vetor/PotraceProcessoService.cs.

    Este projeto (VetorGpl) em si É código GPL — o código-fonte dele tem que continuar
    disponível sob GPL-3.0-or-later pra quem receber o binário (ver LICENSE-GPL-3.0.txt
    nesta pasta). NÃO é aconselhamento jurídico definitivo — confirmar com um advogado
    antes de distribuir.
  -->

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>VetorGpl</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="BitmapToVector.SkiaSharp" Version="1.16.3" />
    <PackageReference Include="SkiaSharp" Version="2.88.8" />
  </ItemGroup>

</Project>
